[Unit]
Description=Cloak server 
After=syslog.target network.target

[Service]
ExecStart=/opt/cloak/ck-server -c /opt/cloak/config/config.json
Restart=always
RuntimeMaxSec=3600

[Install]
WantedBy=multi-user.target